いい質問です。
相手は「実装を止めないための前提固定」をしたがっているので、ここでは **迷う項目を先に潰して、実装判断を相手に委ねない** のが大事です。

目的は「相談」ではなく、**AIがそのまま手を動かせる初期条件を固定すること**です。
なので回答方針は **“今決める / 後回しにする / 仮で進める” を明確に切る** のが正解です。

以下、そのまま返せる形で整理します。

---

# 開発前確認事項への回答（ClassForge MVP）

## 1. 技術スタック関連

### 1. Tailwind CSSのバージョン

**Tailwind CSS v4 を採用してください。**

理由：

* 新規開発のため v3互換維持を優先する理由がない
* Cursor / AI補助との相性が良い
* 今後の保守性を優先

v4前提で構築してください。

---

### 2. Node.jsのバージョン

**Node.js 22系（LTS）前提** で進めてください。

ローカル環境差異は後で吸収するので、実装は Node 22 基準で統一してください。

---

### 3. Headless CMS

**MVPでは不要です。**

初期MVPでは CMS は入れません。
講座・LP・FAQ は **DB管理（Prisma + PostgreSQL）** で実装してください。

CMS導入は Phase 2 以降（microCMS優先）で検討します。
MVPでは不要です。

---

## 2. 外部サービス関連

### 4. Supabase

**新規作成前提** で進めてください。

まだ本番用の接続先は未確定なので、開発時は `.env.example` と接続前提コードまで作成してください。
実接続値は後で投入します。

つまり：

* Supabase前提で実装
* 実プロジェクト接続は後付け
* Prisma / schema / migration は先に進める

---

### 5. Stripe

**MVPでは Stripe のテストモード実装あり** で進めてください。

非商用PoCでは決済なし運用も想定していますが、
**MVPコードとしては Stripe Checkout（test mode）まで実装対象** です。

本番課金は後。
ただしコード上は Stripe を組み込みます。

---

### 6. Resend

**未作成前提で進めてください。**

実装は Resend 前提で進め、接続値は後から設定します。
認証・購入完了メールは Resend を前提に抽象化してください。

---

### 7. Auth.js送信元メール

**初期は [no-reply@classforge.jp](mailto:no-reply@classforge.jp) 想定で仮置き** してください。

実ドメイン確定前のため、envで差し替え可能な形で実装してください。
固定値埋め込みは禁止です。

---

## 3. 開発範囲・方針

### 8. 今回の開発スコープ

今回は **Phase 1（MVP）全体** を対象にします。

対象：

* 会員登録 / ログイン
* マイページ
* 講座一覧 / 講座詳細
* 動画講座
* 視聴進捗
* Stripe決済
* 商品LP
* 管理画面（講座 / 会員 / 売上）

ただし一括実装ではなく、**依存順で段階実装** してください。

推奨順：

1. 基盤（Auth / DB / Prisma）
2. 講座 / レッスン
3. 動画視聴 / 進捗
4. 決済
5. マイページ
6. 管理画面
7. LP

---

### 9. 動画コンテンツ

初期は **ダミーURLで実装** してください。

MVP段階では YouTube限定公開URL を想定したダミーデータで十分です。
動画基盤は差し替え可能な設計にしてください（将来 Vimeo/Bunny 切替）。

---

### 10. デプロイ先

初期は **ローカル開発優先** で進めてください。

Vercel デプロイは基盤完成後で構いません。
先にローカルで MVP が通る状態を優先します。

---

## 4. デザイン関連

### 11. ブランドカラー / ロゴ

現時点では **未確定** です。
仮デザインで進めてください。

仮UI方針：

* ミニマル
* 白ベース
* アクセントは Indigo 系
* モバイルファースト
* 余白広め
* 情報商材感ではなく SaaS / Learning 系

ロゴはテキストベースの仮置きで問題ありません。

---

### 12. 参考デザイン

デザイン参考は以下の方向でお願いします。

* Thinkific（構成）
* Teachable（導線）
* Udemy（講座一覧）
* Stripe（余白 / UI密度）
* Notion（シンプルさ）

コピーではなく、方向性の参考です。

---

## 5. データ関連

### 13. 講座サンプルデータ

初期は仮のシードデータを作成してください。

サンプル講座：

1. ChatGPT仕事術入門
2. 副業AI活用講座
3. 非エンジニア向け自動化入門

各講座：

* 3〜5セクション
* 各セクション 3〜6レッスン
* 動画 + テキスト概要
* 無料公開レッスンを1つ含む

シード投入まで実装対象です。

---

## 実装時の重要ルール

* 不明点で停止しない
* env未確定項目は `.env.example` 化して先に進める
* 外部サービス未契約でも interface / abstraction まで先に実装
* 仮データで画面を完成させる
* 後から差し替え可能な設計を優先
* 「未確定なので未実装」は禁止

---

## 実装開始条件

この前提で、

1. 不明点ログ作成
2. 実装計画作成
3. ディレクトリ設計
4. DB schema
5. MVP実装

まで進めてください。

不明点は停止理由ではなく、**仮決定を置いた上で進行** を優先してください。

---

この回答の意図はかなり明確で、
「確認ありがとう、ただし止まらず進めて。止まるくらいなら仮置きで進めて」が主旨です。

AI実装ではかなり重要で、
**“未確定＝停止” を許すと永遠に要件確認で終わる** ので、最初にこれを潰すのが正解です。
